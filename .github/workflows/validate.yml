name: Validate QACraft

on:
  pull_request:
    branches:
      - main
    paths:
      - "adapters/**"
      - "catalog/**"
      - "docs/**"
      - "evaluations/**"
      - "qacraft/**"
      - "schemas/**"
      - "shared/**"
      - "skills/**"
      - "scripts/**"
      - "tests/**"
      - "MANIFEST.in"
      - "mkdocs.yml"
      - "pyproject.toml"
      - "qacraft_build.py"
      - "setup.py"
      - ".github/workflows/*.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate on Python 3.12
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Generate documentation
        run: python3 scripts/generate_docs.py

      - name: Validate repository
        run: python3 scripts/validate_repo.py

      - name: Run tests
        shell: bash
        run: |
          set +e
          python3 -m unittest discover -s tests -v > "$RUNNER_TEMP/qacraft-tests.log" 2>&1
          status=$?
          if [ "$status" -ne 0 ]; then
            tail -n 160 "$RUNNER_TEMP/qacraft-tests.log" > "$RUNNER_TEMP/qacraft-test-failure.log"
            cat "$RUNNER_TEMP/qacraft-test-failure.log"
            exit "$status"
          fi
          tail -n 20 "$RUNNER_TEMP/qacraft-tests.log"

      - name: Upload test failure diagnostic
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: qacraft-test-failure
          path: ${{ runner.temp }}/qacraft-test-failure.log
          if-no-files-found: ignore
          retention-days: 1

      - name: Validate public QACraft 1.4.1 release
        env:
          QACRAFT_PUBLIC_REPORT: ${{ runner.temp }}/QACRAFT_V1.4.1_PUBLIC_VALIDATION.md
        run: python3 tests/public_release_validation.py

      - name: Upload public release validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qacraft-1.4.1-public-validation
          path: ${{ runner.temp }}/QACRAFT_V1.4.1_PUBLIC_VALIDATION.md
          if-no-files-found: warn
          retention-days: 3

      - name: Check generated files are committed
        run: git diff --exit-code
