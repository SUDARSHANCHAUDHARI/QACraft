[project]
name = "qacraft"
version = "1.1.0"
description = "Installable, verifiable QA skills and deterministic behavior evaluations"
requires-python = ">=3.10"
dependencies = []

[tool.qacraft]
catalog = "catalog/skills.json"
docs = "docs/index.html"
validator = "scripts/validate_repo.py"
